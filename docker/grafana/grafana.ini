[feature_toggles]
# Disable backend migration for InfluxDB v1.x — the new backend generates
# FROM "default"."measurement" but the actual retention policy is "autogen",
# causing all panel queries to return empty results.
influxdbBackendMigration = false
